项目专属上下文与经验沉淀
1. 项目基础信息（新建项目时手动填一次）
状态管理方式：[如：React useState / Vue ref / Pinia]
组件通信方式：[如：Props传递 / 事件总线 / 状态管理库]
核心数据流向：[如：父组件获取数据 -> 传给子组件展示]
部署环境：[如：Vercel / Netlify / 本地开发]
2. 核心依赖关系图（新建项目时手动填一次）
[文件A] 被 [文件B, 文件C] 依赖，改它时必须验证后者
[API接口X] 被多个页面调用，修改前必须评估影响
[关键组件Y] 影响全局样式/状态，修改需谨慎
3. 已知陷阱日志（⚠️每次踩坑后必须追加！）
格式：- [日期] 错误现象 | 错误尝试 | 正确方式 | 根本原因 | 避免策略
示例：

[0615] 点击按钮无响应 | 直接给按钮重新绑定事件 | 在状态更新后使用回调处理 | 异步状态未更新导致事件丢失 | 状态更新后延迟执行事件绑定
[0616] 接口报401 | 把token写死在header里 | 改为从localStorage动态读取 | 违反安全红线且token过期 | 使用环境变量存储token，动态获取
[2026-04-17] Supabase 400错误+数据重复 | 1.移除字段 2.添加字段 | 从select查询中移除jsonb字段 | jsonb字段不能直接放在select查询中 | 查询前检查字段类型，避免直接查询jsonb
[2026-04-18] Worker初始化SecurityError | 使用绝对路径 | 双模式Worker：file://用Blob URL，http/https用文件 | 浏览器同源策略限制 | Worker路径需根据协议动态处理
[2026-04-18] 新用户登录失败提示"无法获取角色信息" | 批量插入SQL语法错误 | Auth创建用户+获取UUID+插入user_roles表 | 权限页与登录页数据不互通 | 统一使用Auth API，避免数据不一致
[2026-04-18] SQL插入报错"duplicate key value violates unique constraint" | 重复执行INSERT | 先查询确认记录是否存在，存在则用UPDATE | user_id有唯一约束 | 操作前先检查记录是否存在
4. 成功经验日志（✅每次成功后记录）
格式：- [日期] 成功场景 | 解决方案 | 关键点 | 复用价值 | 适用场景
示例：

[0618] 实现数据表格分页 | 虚拟滚动 + 分页API | 只渲染可见行，减少DOM节点 | 可复用于任何大数据列表 | 数据量大的表格展示
[0619] 优化图片加载速度 | 懒加载 + 压缩 + CDN | 首屏加载时间从3s降到1s | 适用于所有图片密集型页面 | 图片较多的页面
[2026-04-17] 解决Supabase jsonb查询问题 | 使用select语句时排除jsonb字段 | 避免直接查询jsonb类型字段 | Supabase jsonb字段处理 | 需要查询包含jsonb字段的表
[2026-04-18] 成功实现Worker双模式加载 | 根据协议动态选择加载方式 | 解决file://和http/https的兼容问题 | 需要使用Worker且支持本地开发 | 本地开发和生产环境的Worker加载
- [2026-04-10] GitHub Pages 部署后 404 错误 | 尝试用脚本自动推送失败，仅添加 index.html | 手动上传所有缺失 HTML 文件，确保目录结构一致 | 仓库只上传了部分文件，缺少 booking.html 等关键文件
- [2026-04-10] JS/CSS 文件 404 | 尝试修改代码路径 | 创建 css/、js/ 子目录，保持代码路径不变 | 本地文件平铺，代码使用子目录路径，结构不一致
- [2026-04-19] JS语法错误Unexpected end of input | 优化图表时添加else分支但未闭合 | 在else块结束后添加闭合大括号 } | 代码块嵌套时遗漏闭合符号
[2026-04-19] 自定义日期范围功能回滚 | 删除新增的CSS样式、HTML结构和JS函数 | 恢复原始日期筛选逻辑 | 功能异常或需求变更 | 保留原始代码备份，使用Git版本控制